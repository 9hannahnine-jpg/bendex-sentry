[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn proxy_sentry:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "bendex-sentry"
