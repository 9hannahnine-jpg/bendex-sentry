[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn sentry_v16:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "bendex-sentry"
